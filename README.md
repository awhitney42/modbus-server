# modbus-server
A simple Modbus TCP server that acts as a gateway for serial data.

Every 3 bytes read from the serial port are sent to 3 one-byte holding registers on the Modbus server.

See also: [Modbus TCP Gateway for the Commodore 64](https://adamwhitney.net/modbus-tcp-gateway-for-the-commodore-64/)
